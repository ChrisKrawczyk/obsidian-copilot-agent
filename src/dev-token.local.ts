// =============================================================================
// PHASE 2 DEVELOPMENT TOKEN
// =============================================================================
// This file is committed as a PLACEHOLDER so clean checkouts build.
//
// To use the plugin in your local Obsidian install:
//   1. Run `gh auth token` (or generate a PAT) and paste the value below.
//   2. Run: `git update-index --skip-worktree src/dev-token.local.ts`
//      so your real token does not get committed.
//   3. Rebuild (`npm run build`) and copy main.js to your vault plugin folder.
//
// Phase 3 (Device Flow OAuth) replaces this file entirely. Until then, treat
// the local copy of this file as a credential.
// =============================================================================

export const DEV_TOKEN = "REPLACE_WITH_YOUR_GHO_TOKEN";
